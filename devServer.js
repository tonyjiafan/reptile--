'use strict'

let number = Math.random();
    console.log('取0 ~ 1 之间的随机数' + Math.random())
    console.log('把数四舍五入为最接近的整数=>' + Math.round(number))
    console.log('向下取整=>' + Math.floor(number))
    console.log('向上取整=>' + Math.ceil(number))
    console.log('绝对值=>' + Math.abs(number))
    console.log('返回数的自然对数=>' + Math.log(number))
    console.log('Math 对象的原始值=>' + number.valueOf())
